import java.util.HashSet;
import java.util.Set;
public class A5 {
    public static void main(String[] args) {
        // Set<Integer> s1 = new HashSet<>();

        // s1.add(1);
        // s1.add(2);

        // Set<Integer> s2=new HashSet<>();

        // s2.add(2);
        // s2.add(3);

        // s1.addAll(s2);

        // System.out.println(s1);

        // HashSet<Integer> set1=new HashSet(20,0.5);//16 by default //by default load factor is 0.75

        //Capacity->no of elements set can store, by default 16
        //Load Factor -> whenever our set is filled 50%, a new set of double capacity will be created
        // and all elements will be moved. By default load factor is 75%
    }
}
